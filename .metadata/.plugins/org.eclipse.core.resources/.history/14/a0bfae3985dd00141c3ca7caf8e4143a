package control;

import greenfoot.Actor;

public class Car extends Actor {

	public Car() {
		super();
		this.setImage("images/regular/blueCar.png");
	}

	@Override
	public void act() {
		if (!this.isAtEdge()) {
			this.setLocation(this.getX(), this.getY() + 1);
			System.out.println(this.getRotation());
			if(this.getRotation() < 91){ 
				this.turn(1);
			}
		}
	}
}
